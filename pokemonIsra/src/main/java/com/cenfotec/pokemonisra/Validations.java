import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

public class PokemonTest {

    @Test
    public void testAtaque() {
        Pokemon pokemon = new Pokemon();
        boolean resultado = pokemon.atacar("http://ec2-3-18-23-121.us-east-2.compute.amazonaws.com:8080/api/gimnasio/atacar");
        assertTrue(result);
    }
}

    @Test2
public class PokemonIsraApplicationTests {
this. ec2= "http://ec2-3-18-23-121.us-east-2.compute.amazonaws.com:8080/api/gimnasio/unirse"
if {assertTrue ();
    else{"success": true,
    "message": "The battle and player were added."}
}